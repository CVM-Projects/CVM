#ifndef InstCode
#define InstCode(x)
#endif

#ifndef InstCodeDebug
#define InstCodeDebug(x)
#endif

InstCode(nop)
InstCode(mov)
InstCode(load)
InstCode(loadp)
InstCode(tset)
InstCode(cpyn)
InstCode(call)
InstCode(ret)
InstCode(jump)

InstCodeDebug(opreg)

#undef InstCode
#undef InstCodeDebug
